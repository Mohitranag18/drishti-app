'use client';
import React from 'react';
import ProtectedRoute from '../components/ProtectedRoute';
import ScreenWrapper from '../components/ScreenWrapper';
import GlassCard from '../components/GlassCard';
import Button from '../components/Button';
import { useApp } from '../context/AppContext';

const HomeScreen = () => {
  const { dispatch } = useApp();

  return (
    <ProtectedRoute>
      <ScreenWrapper>
        <div className="space-y-6">
          <GlassCard>
            <h1 className="text-3xl font-bold text-white mb-4">Welcome to Drishti</h1>
            <p className="text-white/80 mb-6">
              Your mental wellness companion. Find new perspectives on your challenges and grow stronger.
            </p>
            <Button 
              onClick={() => dispatch({ type: 'SET_VIEW', payload: 'perspective' })}
              className="w-full"
            >
              Start New Session
            </Button>
          </GlassCard>

          <GlassCard>
            <h2 className="text-xl font-semibold text-white mb-4">Quick Actions</h2>
            <div className="grid grid-cols-2 gap-4">
              <Button 
                onClick={() => dispatch({ type: 'SET_VIEW', payload: 'journal' })}
                variant="secondary"
              >
                Journal
              </Button>
              <Button 
                onClick={() => dispatch({ type: 'SET_VIEW', payload: 'notifications' })}
                variant="secondary"
              >
                Notifications
              </Button>
            </div>
          </GlassCard>
        </div>
      </ScreenWrapper>
    </ProtectedRoute>
  );
};

export default HomeScreen;
